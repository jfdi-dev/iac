artifact: app

deployment:
  policies:
    custom:
      document-datastore-create:
        - effect: Allow
          actions: ['dynamodb:*']
          resources: ['*']
    managed:
      - AmazonRoute53FullAccess
      - AWSLambda_FullAccess
      - IAMFullAccess
      - AmazonS3FullAccess
      - AmazonAPIGatewayAdministrator
      - AWSCertificateManagerFullAccess
      - CloudFrontFullAccess
      # This is for Auth submodule:
      - SecretsManagerReadWrite

secrets:
  - auth0
